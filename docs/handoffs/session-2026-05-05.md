---
date: 2026-05-05
chat: nce_automation (this repo)
participants: Gus + Claude (Opus 4.7), with parallel coordination from a Claude session in nce-site
---

# Session handoff — 2026-05-05

## What shipped (live on `main`)

| Commit | What |
|---|---|
| db change | Norman + Richard promoted to `staff_users.role = 'admin'` (they were `staff` and missing Finance/Settings; saved as memory `project_owner_admin_roles.md`). |
| `92a4c10` | **Strategic product edit (Phase 2)** — `/products/[id]/edit-strategic` page + form, photo manager (upload/reorder/delete via Supabase Storage), `PATCH /api/products-strategic/[id]` orchestrator that updates Supabase + QBO Item only (no Shopify), belt-and-braces redirects between bridge `/edit` and `/edit-strategic`, sidebar regex tightened. Verified end-to-end via Playwright + dev-log monitor. |
| `2dbbe59` | Sidebar "+ New product" moved to top of Strategic group. |
| `0ffc68d` | **Production-safety guardrails for inventory + sales sync** — plain-English callouts in `docs/plans/now-vs-strategic.md` §12.2 (production-safety rules) + §13 (cutover-day flag flips with the rationale), two new hard rules in `CLAUDE.md` so future chats can't ship Phase 2 ungated, mirrored summary in `nce-site/docs/PRD.md` §3.11. |
| `25e8cd9` | **§13.5 Repo consolidation plan** — both Claude sessions (this repo and nce-site) aligned: merge `nce_automation` into `nce-site/app/(admin)` post-cutover, after `SHOPIFY_SYNC_ENABLED=false` has soaked 1 week. Full 10-step sequence + rollback windows documented. |
| `486840a` | **Phase 0: QBO inventory shadow read** — migration adds `products.qbo_qty_on_hand` + `qbo_qty_pulled_at`, new `lib/qbo/inventory.ts` with paginated `findItems`, cron at `/api/cron/qbo-inventory-pull` every 10 min (vercel.json), logs to `sync_log`. Smoke test: 2,929 QBO items scanned in 7.8s, 60 of our products linked, 37 in drift vs `stock_quantity`. |

## Current state of the cutover queue (`docs/plans/now-vs-strategic.md` §9)

**Done:** items 1–5.

**Next up:** **#6 Image hosting migration (§12.3)** — bulk download every product image off Shopify CDN, upload to Supabase Storage (`product-images` bucket), update `product_images.src` to the new public URL. ~1–2 sessions. Has to land before cutover or every PDP goes blank-image.

After that: #7 metafields editor → #8 inventory Phases 1+2 → #9 strategic finance → #10 CMS → #11 draft orders → #12 inventory Phase 3 (refunds + backfill) → #13 cutover → §13.5 repo merge.

## Phase 0 soak — what to watch

The 10-min cron is now running on production. Per §12.2 we need <1% drift on linked products before promoting to Phase 1. Quick check:

```sql
SELECT details->>'driftCount' AS drift,
       details->>'productsLinked' AS linked,
       details->>'qboInventoryItems' AS qbo_items,
       created_at
FROM sync_log
WHERE action='qbo_inventory_pull'
ORDER BY created_at DESC LIMIT 10;
```

The drift ratio will look bad until cutover (most products have no `qbo_item_id` until then). What we're really watching for in the soak is: do successful runs come through every 10 min, do drift values stabilize on the linked subset, are there any runs with `status='error'` in `sync_log`.

## Things future sessions must not get wrong

- **Phase 2 (sale → QBO Sales Receipt) MUST be gated on `SHOPIFY_SYNC_ENABLED=false`.** Otherwise the bridge payout JE + the new Sales Receipt double-book sales income. Hard rule in `CLAUDE.md`.
- **Phase 1 (cron writes to `stock_quantity`) requires owner sign-off** before flipping on — overwrites manual stock edits. Hard rule in `CLAUDE.md`.
- **Repo merge happens post-cutover, week 2 of soak.** Drafted as §13.5 with a 10-step sequence. Don't relitigate timing — both Claude sessions converged on this.
- **Bridge folders are frozen.** Don't edit them as part of Strategic work (`app/products/new/`, `app/products/[id]/edit/`, `app/finance/`, `app/api/cron/sync/`, `lib/shopify/`, `lib/sync/payouts.ts`, `lib/qbo/items.ts`).
