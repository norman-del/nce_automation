# Carrier connections schema scaffold — 2026-04-28

PRD §3.7 prep. Migration `20260428160000_carrier_connections.sql` adds two tables to Supabase: `carrier_connections` (per-carrier config — code, display name, account number, API endpoint, AES-256-GCM encrypted credentials, active/test_mode flags, health-check fields, unique on `carrier_code`) and `shipping_labels` (per-order label storage referencing `orders(id)` with carrier_code, tracking number, label URL/PDF bytes, weight, service level, cost in pence, raw response JSON, indexed on order_id and tracking_number). What's NOT in this commit: no UI, no API calls, no adapter code, no seed rows, no RLS policies — just the storage shape so future work is unblocked. Next session needs to: (1) research APC Partner Services API access (sales contact, sandbox creds, doc PDF), (2) build `lib/carriers/apc.ts` adapter with `lib/crypto.ts` for credential encrypt/decrypt, (3) add a Settings → Carriers tab to manage connections + run health checks, (4) add a "Book shipping" panel to the Order detail page that creates a `shipping_labels` row and writes the tracking number back to `orders.tracking_number`. Pallettrack adapter follows the same shape once APC is proven.
